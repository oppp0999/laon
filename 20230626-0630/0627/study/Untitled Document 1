1. 가변인자 매크로 - Logger.h
c++11 버전에서 ##__VA_ARGS__ 을 이용하여 가변인자를 사용할 수 있다. 그때
디폴트값을 true 설정하여 인자값이 없을 때도 동작하게 만듦

내부 함수에서는
 /* Logging enable */
	if(is_enable == false)
		return;

으로 가변인자 값을 받게되면 추가로 처리해준다.

2. 마리에db에서는 true false 값이 없고 모든 값을 바챠로 문자열로 가져온다.

그래서 변환하는 작업이 필요

